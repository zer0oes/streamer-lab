node_modules/
.npm-cache/
.env
npm-debug.log*
.DS_Store
# library/*/zer0oes-*

# Bibliotheque de medias locale (images/videos uploadees, cf. lib/media.mjs) :
# binaires potentiellement lourds, contrairement au reste de library/ (JSON/
# HTML/CSS/JS) deja suivi par git.
library/media/

# Compiled CSS — source of truth is styles/*.scss, run `npm run build:css`
# (or `npm run dev`, which does it automatically) to regenerate.
/public/styles.css
/public/styles.css.map

.vscode/sidebar-notes.json